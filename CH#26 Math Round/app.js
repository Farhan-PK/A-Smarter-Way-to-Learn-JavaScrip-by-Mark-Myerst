var roundNumber=4.58654;
roundNumber=Math.round(roundNumber);
roundNumber=Math.ceil(roundNumber);
roundNumber=Math.floor(roundNumber);
console.log(roundNumber);

/*
Math.round() it convert to 1.5 into 2 and 1.4 into 1;
Math.floor() it convert to 1.5 into 1, it fource full downgrade the number;
Math.ceil() it convert to 1.5 into 2, it fource full upgrade the number;

*/

